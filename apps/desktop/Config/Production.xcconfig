// Production.xcconfig
// Build settings for the shipping Zerro app. Wired to the Debug and Release
// configurations of the app target (see Zerro.xcodeproj). These values flow
// into Info.plist via $(...) references (SUFeedURL) and
// set the product's identity (bundle id, product name).
//
// NOTE on URLs: xcconfig treats `//` as a comment, which would swallow the
// `https://` scheme. The `$()` between the scheme's slashes is an empty
// variable reference — it parses without starting a comment and expands to
// nothing, so the resolved value is a normal `https://…` URL.

SU_FEED_URL = https:/$()/getzerro.app/appcast.xml
PRODUCT_BUNDLE_IDENTIFIER = com.cbreeding.Zerro
PRODUCT_NAME = Zerro

// Custom URL scheme LemonSqueezy bounces the checkout back to
// (zerro://checkout-complete). Flows into Info.plist's CFBundleURLSchemes via
// $(DEEPLINK_SCHEME) and is read back by AppDelegate to gate the inbound deep
// link. Staging overrides this so a checkout callback can never cross between
// the side-by-side prod and staging installs.
DEEPLINK_SCHEME = zerro

// Build channel marker surfaced in Info.plist (ZerroBuildChannel). "community"
// is the default for every build from a plain source checkout; the release
// workflows override this to "official" on the xcodebuild command line,
// together with the OFFICIAL_BUILD Swift compilation condition. Deliberately
// NOT settable to "official" from any checked-in configuration.
ZERRO_BUILD_CHANNEL = community

// Swift ⇄ C bridge for the official-build marker (Zerro/OfficialBuildMarker.c).
// Set here so every configuration (Debug and Release via Production.xcconfig,
// Staging via Staging.xcconfig) imports the same header.
SWIFT_OBJC_BRIDGING_HEADER = Zerro/Zerro-Bridging-Header.h
