# Xcode user-specific state
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# Build / release artifacts (regenerated on build & packaging)
dist/

# Secrets / local env — never commit. (Backend secrets live in Supabase, not the repo.)
.env
.env.*
!.env.example
