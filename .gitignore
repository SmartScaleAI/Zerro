# macOS
.DS_Store

# editors
.vscode/
.idea/

# Claude Code local tooling (dev-launch configs, etc.)
.claude/

# FUSE orphan files (created when files are edited while open; runtime junk)
.fuse_hidden*

# LLVM coverage output (generated on instrumented runs)
*.profraw
